# My_Neural_Network
A fully functional, multi-layer neural network built entirely from scratch using Python and NumPy. This project implements core deep learning components—including forward propagation, backpropagation, activation functions, dropout regularization, and the Adam optimizer
